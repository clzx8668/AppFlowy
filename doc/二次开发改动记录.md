# 二次开发改动记录（相对上游）

> 记录本 fork（`origin = github.com/clzx8668/AppFlowy`）相对上游 AppFlowy 的每一处改动，
> 用于后续合并上游 Core 修复时快速定位差异。规则见仓库根 `AGENTS.md`。
> 所有改动只在前端适配层，`frontend/rust-lib` 内核零改动。

## 2026-09-23 · 步骤①：本地优先模式（跳过登录 + 隐藏云端入口）

**背景**：上游 0.11 默认连接 AppFlowy Cloud，当前网络下 `appflowy.cloud` 被 Cloudflare 403，
真机启动后卡在登录页无法进入工作区；且产品规划已明确放弃 AppFlowy Cloud、走全本地 + WebDAV 快照同步。

**做法**：新增一个显式开关 `kLocalFirstMode`，集中控制"全本地优先"，其余 4 处只在既有条件分支上收口。

| 文件 | 改动 |
| --- | --- |
| `lib/env/local_first.dart`（新增） | 本地优先开关 `kLocalFirstMode`，是本次唯一"总闸"；置 `false` 即回退到上游行为 |
| `lib/main.dart` | 启动改为 `runAppFlowy(isAnon: kLocalFirstMode)`：匿名模式自动创建/复用本地用户，不请求云 |
| `lib/env/cloud_env.dart` | `AppFlowyCloudSharedEnv.fromEnv()` 在本地优先模式下直接返回 `AuthenticatorType.local` 与空云配置；于是 `isAuthEnabled` / `isAppFlowyCloudEnabled` 均为 false，云同步任务不初始化、账号登录/订阅/协作入口按上游既有条件自动隐藏 |
| `lib/user/presentation/screens/splash_screen.dart` | 未启用云鉴权时不再把移动端强制跳到登录页（上游 `isAuthEnabled \|\| UniversalPlatform.isMobile` 会让本地模式在手机上卡在登录页） |
| `lib/workspace/presentation/settings/widgets/settings_menu.dart` | 未启用云时隐藏"云服务设置"菜单项 |
| `lib/mobile/presentation/home/tab/space_order_bloc.dart` | 未启用云时隐藏移动端 `Shared`（协作/共享）标签页，并让被隐藏的默认标签回退到首个可见标签，避免 `TabController` 初始索引越界 |
| `doc/tools/android-rust-build.sh` | 修复：不再清空 `jniLibs` 目录（会删掉 CMake 放入的 `libc++_shared.so`，导致真机 `dlopen failed: library "libc++_shared.so" not found`），并在缺失时自动从 NDK 补齐 |

**验证**（荣耀 ELZ-AN10，Android 14）：

- 启动日志：`restart AppFlowy: isAnon: true`、`cloud setting: authenticator: AuthenticatorType.local`、
  云配置全空 `{base_url: , ws_base_url: , gotrue_url: ...}`，无任何云请求、零异常；
- 启动耗时从 4.2s 降到 0.8s（少了云相关初始化）；
- 界面：直接进入本地工作空间（空间/最近/收藏），可正常新建与编辑笔记，重装 APK 后本地数据保留；
- 回归：`dart analyze`（改动文件）无告警；`flutter build windows --debug` 通过。

**后续待办**（本步骤遗留）：

1. 桌面端侧边栏「Shared/发布站点/邀请成员」等云协作入口尚未逐一核对隐藏。
2. 移动端首页设置弹窗（`HomePageSettingsPopupMenu`）内的云相关项待核对。
3. 登录/注册路由与页面代码保留未删除（按规约"只隐藏不删除"）。

## 2026-09-23 · 步骤①收尾：清空剩余云端/协作入口

**做法**：仍以 `kLocalFirstMode` 为准，逐屏核对后把仍在显示的云端入口收口；并修掉一个"重启丢模式"的隐患。

| 文件 | 改动 |
| --- | --- |
| `lib/startup/startup.dart` | `runAppFlowy()` 的 `isAnon` 默认值改为 `kLocalFirstMode`。此前内部重启入口（登出、切换数据目录、错误页重试等 18 处调用）用的是默认 `false`，会退回非本地模式并落到欢迎/登录流程 |
| `lib/mobile/presentation/home/mobile_home_setting_page.dart` | 隐藏 `CloudSettingGroup`（Cloud settings / Cloud server）；隐藏 `UserSessionSettingGroup`（退出登录、第三方登录）——本地模式没有账号概念 |
| `lib/mobile/presentation/setting/personal_info/personal_info_setting_group.dart` | 隐藏「You're in local mode + Login to AppFlowy Cloud」按钮 |
| `lib/mobile/presentation/setting/launch_settings_page.dart` | 隐藏 `SelfHostSettingGroup`（自建服务端配置） |
| `lib/workspace/presentation/home/menu/sidebar/footer/sidebar_footer.dart` | 桌面侧边栏隐藏「模板」（指向 appflowy.com/templates）入口 |

**核对结论（逐项）**：

| 位置 | 结论 |
| --- | --- |
| 移动端首页右上「⋯」菜单 | 成员/邀请入口本来就由 `workspaceType == ServerW` 控制，本地模式已不显示（截图确认只剩 设置/回收站/帮助/支持） |
| 移动端设置页 | 已无 Cloud settings、无 Login to AppFlowy Cloud（真机截图确认） |
| 移动端首页标签栏 | `Shared` 标签页已隐藏（上一步） |
| 桌面侧边栏 | 本地模式下走 `SidebarFolder`（非空间/协作布局），无 Shared 分区、无工作空间切换器、无升级提示；模板入口已隐藏 |
| 桌面设置弹窗 | 云服务设置菜单项隐藏（上一步）；账号页登录区由 `isAuthEnabled` 控制，本地模式不显示 |
| 文档页「分享」按钮 | **保留**：本地模式下 `enablePublish == false`，该菜单只剩「导出为」等本地功能（改名/隐藏与否待定） |
| 未处理（非协作类，待确认） | 移动端设置页「支持」组（Discord/上报问题）、「关于」组（隐私政策/条款）等外部链接；桌面端「获取支持」入口 |

**验证**：

- 真机（荣耀 ELZ-AN10）：重新安装后逐屏截图核对，设置页与首页菜单均无云端/登录入口；应用正常进工作区、可编辑笔记。
- Windows 桌面端：`flutter build windows --debug` 通过；应用数据目录为 `%APPDATA%\io.appflowy\AppFlowy\data_dev`（本地），
  昨日云模式用的是 `data_dev_beta.appflowy.cloud`，说明桌面端也已切到本地模式；Rust 日志中无 403/登录相关记录。
  注：桌面窗口不在前台时 Flutter GPU 表面抓不到完整帧，侧边栏底部区域未能像素级截图确认（代码层面已收口）。

## 2026-09-23 · 步骤①收尾（二）：移除外部链接

| 文件 | 改动 |
| --- | --- |
| `lib/mobile/presentation/home/setting/settings_popup_menu.dart` | 首页「⋯」菜单移除 Help & documentation（appflowy.com/guide）与 Get Support（Discord） |
| `lib/mobile/presentation/setting/support_setting_group.dart` | 「支持」组移除 Join Discord 与 "Report issue on GitHub"（指向 AppFlowy-IO 上游仓库），保留清空缓存 |
| `lib/mobile/presentation/setting/about/about_setting_group.dart` | 「关于」组移除隐私政策 / 条款和条件（appflowy.com 外链），保留版本号 |

核对：真机「⋯」菜单现在只剩 设置 / 回收站；设置页只剩 我的账户(User name)、外观、语言、支持(清空缓存)、关于(版本)。

## 2026-09-23 · 步骤②：现有模块复用分析 + 五个扩展包骨架

**分析文档**：新增 `doc/现有模块复用与扩展分析.md`，结论要点：

- 类 Notion 块编辑器 / 双链 / 块引用 → **直接复用**（不重写编辑器）；
- 视图插件系统（`PluginType` / `registerPlugin` / `PluginBuilder`）与自定义块注册点
  （`editor_configuration.dart` 的 `buildBlockComponentBuilders`）→ **新增页面与自定义块不用改内核**；
  但后端 `ViewLayoutPB` 只有 5 种布局，**新增"视图布局"需要改 Rust，属于红线**；
- AI → 有 `AIRepository` 抽象层，**接口复用、实现可替换**（`app_ai_ext` 只做模型接入与记忆编排）；
- 闪念 / 日记 / CRM / WebDAV 快照 → 上游**完全没有**，必须自研；WebDAV 可复用 `ApplicationDataStorage`
  路径与 `archive` 打包，日期解析可复用 `DateService.queryDate`。

**五个扩展包骨架**（`frontend/appflowy_flutter/packages/`，均为 path 依赖、名字与蓝图一致）：

| 包 | 骨架内容 |
| --- | --- |
| `app_flash_note` | `FlashNoteCapture` 捕获入口契约（悬浮/下拉/快捷键）、模块标识 |
| `app_diary_time` | `DiaryEntryMeta` 元数据模型、`DiaryService`（打开/创建当日日记、区间索引） |
| `app_crm_biz` | `CrmCustomer` 客户模型、`CrmRepository` 仓储契约（独立业务库） |
| `app_webdav_sync` | `WebDavConfig`、`SnapshotInfo`、`WebDavSyncService`（打包/上传/列举/下载） |
| `app_ai_ext` | `MemoryAiService`（摘要/标签/周期回顾）、`AiDigestResult` |

**接入方式**：`pubspec.yaml` 加 5 个 path 依赖；新增 `lib/extensions/extension_registry.dart` 作为 App 与
扩展包之间唯一出入口（后续路由、自定义块、插件注册、AI 实现替换、入口挂载都集中在此），
并在 `PluginLoadTask` 中调用 `ExtensionRegistry.registerAll()` 完成登记。

**验证**：`flutter pub get` 通过（新增 5 个依赖）；`dart analyze`（扩展包 + 注册表 + 改动文件）零告警；
`flutter build windows --debug`、`flutter build apk --debug` 均通过并已安装真机，真机运行正常（扩展包登记日志正常输出）。

## 2026-09-24 · 步骤③：业务库（独立 Sqlite）+ 闪念模块 v0（真机验证通过）

**技术选型（已确认）**：业务数据用 **独立 Sqlite**（`sqlite3` + `sqlite3_flutter_libs`），放在
`<应用数据目录>/business/business.db`，与内核 Core 库（`data_dev/<uid>/`）完全分离。

**新增基础设施包 `app_biz_store`**：

- `BusinessDatabase.open(directory, migrations)`：连接管理 + `PRAGMA user_version` 版本化迁移；
  WAL 模式（便于后续快照一并拷贝）、`foreign_keys=ON`；各业务包自带迁移语句，基础包不含业务表。

**新增 `app_flash_note` 模块（v0 闭环已在荣耀真机跑通）**：

| 组成 | 说明 |
| --- | --- |
| `FlashNote` / `FlashNoteRepository` | 闪念表 `flash_notes`（id/text/created_at/document_id/archived），业务库读写 |
| `FlashNoteService` | 捕获→入库→落成文档→回看；归档/删除；文档创建失败时原文仍安全留在库中 |
| `FlashNoteCapturePage` | 打开即输入的极速捕获页（含"同时落成知识库页面"开关） |
| `FlashNoteInboxPage` | 收件箱：倒序列表、状态（已在知识库/仅记录）、转入知识库、归档、删除 |
| 入口 | 移动端首页「⋯」菜单新增「闪念速记」（AppBar「+」与 FAB 双入口） |

**内核 API 使用（均为公开 API，未改内核）**：

- 带正文建页：`ViewBackendService.createView(initialDataBytes:)`，`initial_data` 必须是
  `DocumentDataPB` 的**二进制 protobuf**（用 `writeToBuffer()`；用 JSON 会被后端拒绝：`incorrect tag`）；
  正文由 `DocumentDataPBFromTo.fromDocument(Document.blank()..insert(...))` 生成。
- 打开发文档：`ViewBackendService.getView(viewId)` + 移动端 `context.pushView(view)`。

**真机验证（荣耀 ELZ-AN10）**：捕获 → 收件箱出现条目 → 转入知识库 → 条目状态变「已在知识库」→
点击条目打开内核文档（标题 + 正文均正确）；业务库文件已从设备拉出核对：
`tables=[flash_notes]`、`user_version=1`、`journal_mode=wal`、行内含 `document_id` 关联。

**已知问题（下一步）**：

1. **闪念文档在移动端侧边栏的落点**未定：通过 `ViewBackendService.createView` 创建的视图，
   文档数据已生成（`document_id` 已回写业务库、可直接打开），但在"本地工作空间 + 移动端侧边栏"
   这一组合下不一定出现在列表里（早期 3 条挂在用户已有页面下时是可见的，之后改为挂在
   workspaceId / 容器页下则不可见）。下一步二选一：
   (a) 用**上游"新建页面"同一路径**建视图（`WorkspaceService.createView(viewSection: Private, viewId: ...)`），
   再用 `DocumentEventCreateDocument(CreateDocumentPayloadPB(initialData: ...))` 写入正文
   （该 payload 支持 initialData，两个 API 均为上游既有路径）；
   (b) 先加一行临时日志打印 `getPrivateViews()` 的 id/name/parentViewId，确定本地工作空间真正的容器视图 id 后再建页。
2. 真机上遗留了 7 条测试闪念与若干测试页面（`flashnoteTEST3` 等），可直接在 App 内删除。
3. 自定义块（心情/天气/位置/倒计时/CRM 卡片）尚未实现，仍待 `editor_configuration.dart` 挂载点接入。

## 2026-09-24 · 步骤④：容器落点定位与修正（真机验证通过）

**产品决策（与用户确认）**：

1. 预设容器 `闪念 / 笔记 / CRM / AI` 四个，设置页可新建自定义容器；
2. 容器页保持普通文档（记录列表只在首页主区渲染）；
3. 移动端底部导航精简为 `首页 / 日历 / CRM / AI / 设置`，通知并入抽屉保留；
4. 日记条目默认落「笔记」容器（保持 4 个容器，如需独立改一行清单即可）。

**关键发现（之前的"页面不可见"根因）**：**分区（section）不匹配**。
移动端首页列表渲染的是 **Public 分区**（手机端「+」新建页面传的也是 `FolderSpaceType.public`），
而容器最初建在 Private 分区，所以创建成功却不出现在列表里；父节点（`parentViewId`）从头到尾都是对的。

**代码修正**：

- `ContainerRepositoryImpl`：容器改为建在 `ViewSectionPB.Public`，列举也读 `getPublicViews()`；
- 容器标记继续写 `view.extra`（`af_container` / `af_module` / `af_icon` / `af_created_at`），
  用上游同一套「新建页面」路径 `WorkspaceService.createView(...)`；
- 闪念记录挂在「闪念」容器下：`ViewBackendService.createView(parentViewId: 容器id, initialDataBytes: ...)`；
- 移动端首页就绪时自动 `ensureDefaultContainers()`（幂等），无需任何手动操作。

**真机验证（荣耀 ELZ-AN10，05:14–05:15）**：

- 页面树出现 `闪念 / 笔记 / CRM / AI` 四个顶层容器 ✔
- 捕获一条闪念后，「闪念」容器出现子节点（可展开）✔

**遗留**：早期在 Private 分区创建的 4 个同名容器成为不可见的历史数据（不影响使用，后续可加一次性清理脚本）；
真机上仍有测试页面与测试闪念，可在 App 内删除。

## 2026-09-24 · 步骤⑤：移动端首页改造成型（抽屉 + 五项导航 + 容器内容视图）

**问题**：上游移动端首页把"页面树/侧边菜单"当主界面（`MobileHomeScreen` 无抽屉，body 直接是 tab+列表），
与"移动端主屏看内容、分类收进抽屉"的产品定位相反。

**实现**（`lib/extensions/local_home/local_home_shell.dart`，本地优先模式生效）：

| 区域 | 内容 |
| --- | --- |
| 顶栏 | 抽屉按钮 + 当前容器名（可点击切换）+ 新建 |
| 主区 | **当前容器的记录视图**：容器下子页面列表（按最后编辑/创建时间倒序，时间戳按内核"秒"单位换算）+ 下拉刷新 + 空态引导 + FAB 新建 |
| 底部导航 | `首页 / 日历 / CRM / AI / 设置` 五项（日历暂为占位，待日记模块） |
| 抽屉 | 用户信息、容器列表（闪念/笔记/CRM/AI + 自定义，可选中切换）、新建容器、通知、设置 |
| 设置标签 | 容器管理（新建容器）+ 进入完整设置页 |

**关键适配点**：

- `MobileHomeScreen`：本地优先模式直接返回 `LocalHomeShell`（容器初始化在壳内幂等执行）；
- `generate_router.dart`：移动端 `StatefulShellRoute` 的 shell **builder 与 pageBuilder 两处**都要跳过上游
  `MobileBottomNavigationBar`，否则会出现两条底部导航栏（第一次只改了 `builder`，真机上仍然双栏）；
- 记录时间戳：内核返回的是**秒**，展示需 `* 1000` 换算成毫秒（首版显示成 1970 年就是这个原因）。

**真机验证（荣耀 ELZ-AN10，05:27）**：顶栏「闪念 ▾」+ 记录列表（时间正确）+ 五项底部导航 + FAB 新建，
无上游底栏叠加；容器内容读取的是真实数据（此前创建的记录可见）。

**下一步**：抽屉/容器切换的真机回归、CRM 与 AI 标签内容、闪念收件箱并入首页记录视图、日历（日记）模块、
记录视图样式切换（列表/网格/时间线）。

## 2026-09-24 · 步骤⑥：移动端体验补齐（4/5 完成）

### 1. 抽屉与容器切换的真机回归 ✔

- 抽屉：用户信息 + 容器列表（闪念/笔记/CRM/AI + 自定义）+ 新建容器 + 通知 + 设置；
- 切换容器 ✔（切到「笔记」后标题与列表同步切换）；
- 新建自定义容器 ✔（真机创建 `TestDocker` 成功并出现在抽屉，内核日志可见 `create view … name:TestDocker`）；
- 修复：抽屉按钮此前用 `Scaffold.of(context)` 绑到了内层 Scaffold（点不动），改为由外层壳传入 `onOpenDrawer`；
- 修复：新建容器后列表未刷新（`_load()` 只回填预设容器）→ 改为「确保预设 + 拉取全部（含自定义）」。

### 2. 记录视图形态：列表 / 网格 / 时间线 ✔

`_ContainerRecordsView` 增加视图切换（顶栏图标 + 弹出菜单），三种形态真机验证：

- **列表**：圆角卡片 + 标题 + 时间；
- **网格**：两列卡片（大圆角、低饱和 surface 色，iOS18 风格）；
- **时间线**：日期分组标题 + 左侧时间轴圆点/连线 + 卡片（显示时分）。

### 3. 闪念收件箱并入首页 ✔

「闪念」容器的首页视图改为直接渲染**闪念记录（业务库）**：文本 + 时间 + 状态（已在知识库/仅记录）、
⋯ 菜单（转入知识库/归档/删除）、顶栏 ⚡️ 与 FAB 捕获入口。原独立收件箱页不再需要（保留代码但不再入口）。

### 4. CRM：独立业务库表结构 + 客户卡视图（数据层与 UI 已验证；保存交互待人工确认）

- `app_crm_biz` 落地：`crm_customers` 表（id/name/company/stage/owner/phone/note/created_at/updated_at）+
  索引 + `CrmRepositoryImpl`（业务库读写）；
- **重要修复**：业务库迁移改为**按模块记录**（`_biz_migrations` 表）。原实现用全局 `PRAGMA user_version`，
  各模块迁移都从 1 开始编号 → 第二个模块的建表被当成"已应用"跳过（CRM 表一直建不出来）；
- CRM 标签页：阶段筛选 chips（全部/线索/接触/方案/投标/成交/流失）、客户卡片列表、新建客户底部面板（iOS18 风格）；
- 真机验证：`crm_customers` 表已创建 ✔、UI 渲染与阶段 chips 交互 ✔；
  **未完成**：adbd 驱动点击「保存」被该机型输入法与系统手势反复截获（试过直接点、IME 提交后点、回车提交、收起键盘后点），
  因此"保存客户 → 列表显示"未能在自动化中确认，请人工点一次确认（代码路径与闪念落库同源，风险低）。

### 5. 日历（日记模块）——未开始

按计划留到下一步：月历 + 每日日记（内核文档，落「笔记」容器）+ 生活元数据（心情/天气/位置），UI 按 iOS18 风格。

## 2026-09-24 · 步骤⑦：日历与日记模块（v0 真机验证通过）

**新增能力（`app_diary_time` 包 + App 侧装配）**：

| 组成 | 说明 |
| --- | --- |
| `DiaryEntry` / `DiaryRepositoryImpl` | 独立业务库新表 `diary_entries`（date_key 主键、mood/weather/location、document_id、updated_at） |
| `DiaryService` | 打开/创建某天日记、设置心情/天气/位置、按月查询（月历标记） |
| `DiaryDocumentGateway`（App 侧实现） | 日记正文=内核 Document：同容器下同名（日期）复用，否则新建；落「笔记」容器 |
| `_CalendarView`（首页日历标签） | iOS18 风格：大标题月份 + 前后月切换 + 周一起始极简月历 + 今天高亮圆 + 心情色点 + 选中日卡片（记心情/记天气/地点/写日记） |

**真机验证（荣耀 ELZ-AN10，06:57–06:59）**：

- 月历渲染与月份切换、选中日期高亮 ✔；
- **双击某天** → 自动创建并打开当天日记文档（内核编辑器，标题 `2026-09-10`）✔；
- 业务库核对：`diary_entries` 表存在，行 `('2026-09-10', '', '', '', '8e9bd728…')`（document_id 已回写）✔；
- 迁移记录表显示 `crm/1`、`diary/1`、`flash_note/1` 三条 —— 按模块记录迁移的修复生效 ✔。

**本轮发现的设备层限制（影响自动化验证，不影响真机使用）**：
Honor 系统手势区会吞掉屏幕底部约 200px 的 adb 点击（现象：FAB、底部卡片按钮、CRM 保存按钮点击无响应，
而网格、AppBar、弹出菜单正常）。因此以下两项需人工手指确认：
1. CRM「新建客户 → 保存」；
2. 日历「记心情 / 记天气 / 地点」与「写今天的日记」按钮（双击日期已可替代"写日记"）。

**下一步候选**：
- 心情/天气/位置升级为**自定义生活块**（挂 `editor_configuration.dart` 的块构建表），实现块级嵌入；
- 日记的时间线视图与"那年今日"回顾；
- AI 模块接入（`AIRepository` 替换实现 + 摘要/标签/回顾编排）；
- WebDAV 快照同步（双库 + 附件打包、冲突人工选择）。

## 2026-09-24 · 步骤⑧：自定义块（生活记录）——已实现并注册，真机插入待验

**关键技术核实（决定了自定义块是否可行）**：内核 `collab-document/src/blocks/entities.rs` 中
`Block.ty` 是 **String**、`Block.data` 是 **任意 JSON（HashMap<String, Value>）**，
即**自定义块类型可以安全落库、回读、参与快照同步，无需修改内核** ✔（蓝图"自定义块只写 payload"成立）。

**实现**（`app_diary_time/lib/src/blocks/life_meta_block.dart`）：

| 组成 | 说明 |
| --- | --- |
| 节点类型 | `life_meta`，属性 `date/mood/weather/location`（全部写 payload） |
| `lifeMetaBlockNode()` | 程序化创建节点 |
| `LifeMetaBlockComponentBuilder` | 块构建器（iOS18 风格卡片：日期 + 心情/天气/位置 chips） |
| `lifeMetaSlashMenuItem()` + `insertLifeMetaBlock()` | 斜杠菜单项与插入逻辑（插入块后补一个空段落） |
| 属性更新 | 走编辑器 transaction → 内核，落库安全 |

**注册（仅两处适配，不删上游代码）**：

- `editor_configuration.dart`：`_buildBlockComponentBuilderMap` 增加 `LifeMetaBlockKeys.type` 条目；
- `slash_menu_items_builder.dart`：移动端与桌面端斜杠菜单列表追加 `lifeMetaSlashMenuItem()`。

**状态**：`dart analyze` 通过、`flutter build apk --debug` 通过 ✔；
**真机插入验证未完成**（本轮 adb 点击序列未命中编辑器 + 预算耗尽）——下一步第一件事：
打开任意文档 → 输入 `/` 确认「生活记录」出现 → 插入并设置心情 → 重启应用后重新打开文档确认块仍在（落库回读）。

**遗留待办**：块选中（`SelectableMixin` 需实现较多内部成员，v0 暂未接入）、块菜单项（复制/删除之外的块级操作）、
以及第 2–4 项（日记增强 / AI 模块 / WebDAV 同步）尚未开始。

### 步骤⑧ 真机验证结论（2026-09-24 22:00–22:12）：**v0 自定义块类型不可用，入口已暂时收起**

真机实测（荣耀 ELZ-AN10）：

1. 手机端「+」块面板需要**单独接线**（它有自己的硬编码列表 `add_block_menu_item_builder.dart`，
   不走斜杠菜单的 `slashMenuItemsBuilder`）——已接入，「生活记录」能出现在块列表 ✔；
2. 点击后**插入成功、渲染正确**（卡片：日期 + 生活记录 + 心情/天气/地点 chips）✔；
3. **杀掉应用重开同一篇日记后，该块消失** ✗ —— 说明"Block.ty 是 String"只保证**存储结构**可容纳自定义类型，
   但 AppFlowy 文档的**运行时写入/同步路径并不接受未知块类型**（我之前只核对了 entities.rs，未覆盖同步写入路径，
   判断有偏差）。Rust 日志中没有相关报错（该日志级别不记录块写入），因此是从结果反推的。

**处置**：为避免给用户暴露"点了会消失"的功能，已把两处入口（斜杠菜单、移动端「+」面板）**暂时收起**
（代码保留在 `app_diary_time` 包内，并加注释说明）。当前构建与仓库状态正常。

**下一步方案（改为蓝图本就允许的形态）**：把生活元数据做成
**「标准块类型 + 自定义 payload + 覆写该类型的构建器」**：

- 节点类型沿用内核支持的 `callout`（或 `paragraph`），数据写在该节点的 attributes（`af_life_meta: {...}`）；
- 在 `editor_configuration.dart` 里用我们自己的构建器**覆盖** `CalloutBlockKeys.type`：当节点带 `af_life_meta` 标记时
  渲染我们的生活记录卡片，否则回退上游 `CalloutBlockComponentBuilder`；
- 这样既保留"块级 payload"的形态，又完全走内核已支持的块类型，落库/同步安全。

## 2026-09-24 · 步骤⑨：新建动作改为「直接新建普通笔记并进入编辑」

**用户反馈**：FAB 新建时弹出输入窗口（要填标题）没什么用，希望取消，直接新建正常笔记、沿用现成的编辑器。

**改动**：

| 位置 | 之前 | 现在 |
| --- | --- | --- |
| 通用容器视图（笔记/AI/自定义容器）的 FAB 与顶栏「+」 | 弹对话框输入标题 → 再创建 | **直接创建**（`createView(name: '')`，内核显示"未命名页面"）→ 立即打开标准编辑器 |
| 「闪念」容器 | FAB 打开闪念捕获页 | FAB 改为**直接新建普通笔记并进入编辑**；顶栏新增「+」（同一动作）；**快速捕获保留在顶栏 ⚡️** |

**真机验证**：在「笔记」容器点「+」→ 直接进入编辑器（标题占位"未命名页面"、块工具栏正常），无任何中间窗口 ✔

## 2026-09-24 · 步骤⑩：去掉「闪念速记」入口，闪念容器统一为普通容器

**用户要求**：顶栏 ⚡️（闪念速记）功能直接去掉，不需要。

**改动**：

1. 移除「闪念」容器顶栏的 ⚡️ 按钮与对应的捕获页调用；
2. 「闪念」容器不再使用"业务库记录列表"专用视图，改为与其它容器**完全一致**的容器视图
   （容器内页面列表 + 列表/网格/时间线三种样式 + FAB/顶栏「+」直接新建笔记）；
3. 删除专用视图 `_FlashNoteRecordsView`（约 280 行），保持代码干净；
   `app_flash_note` 包与业务库 `flash_notes` 表**保留**（历史数据不丢，后续若要恢复"极速捕获"可直接复用）。

**真机验证**：闪念容器顶栏只剩「抽屉 / 容器名 / 视图样式 / +」；列表显示容器内真实页面
（未命名页面 ×2、走出小巷、有什么不同看看是谁、嗑瓜子鲸落）✔

## 2026-09-24 · 步骤⑪：自定义块改造 v1（callout 承载 + 覆写构建器）

**改造内容**（针对 v0"自定义节点类型重启后丢失"的问题）：

| 层 | 实现 |
| --- | --- |
| 数据载体 | 生活记录块 = **标准 `callout` 节点** + 我们的 payload（`af_life_meta`/`af_date`/`af_mood`/`af_weather`/`af_location`），全部写在 attributes 里 |
| 渲染 | App 侧 `LifeMetaCalloutBlockComponentBuilder` **覆写 `CalloutBlockKeys.type`**：节点带 `af_life_meta` 标记 → 渲染我们的卡片；其它 callout → 原样交回上游构建器（行为不变） |
| 入口 | 斜杠菜单 + 手机端「+」块面板均已重新接入「生活记录」 |
| 包边界 | 节点工厂/斜杠项/插入逻辑放在 App 侧适配器（需要 `CalloutBlockKeys`/`EmojiIconData` 等 App 内部类型）；`app_diary_time` 包只保留 payload 键、判定函数与卡片组件 |

**真机状态**：插入与渲染已确认 ✔（卡片：🌤️ 日期 + 生活记录 + 心情/天气/地点 chips）；
**"重启后仍在"的复测未完成**（本轮 adb 导航点进了另一篇笔记，预算耗尽）——下次第一件事就是复测这一条。

## 2026-09-24 · 步骤⑫：定位并修复"生活记录块重启后丢失"（**已通过真机落库回读**）

### 一、先建立一个可靠的验证手段（不再依赖截图/adb 输入）

问题：collab 本地库是 **RocksDB（键值、内容未压缩但键是哈希）**，adb 注入文本会被搜狗输入法吞进候选栏，
截图只能看到"当下渲染"，无法证明"落库"。为此新增工具
[`doc/tools/inspect_collab_db.py`](tools/inspect_collab_db.py)：把真机 `collab_db` 目录
（`adb exec-out run-as ... tar -cf -`，**必须 exec-out 才能保二进制**）拉到本机后，用 `rocksdict`
只读打开，支持列举键值、按关键字全文检索、按可读片段打印。以后每一步的"落库回读"都用它。

### 二、对照实验（同一篇日记 2026-09-23，同一次会话）

| 插入内容 | 走哪条路径 | 重启后是否在库里 |
| --- | --- | --- |
| 普通文字（键盘输入） | 上游段落 | ✔ 在 |
| **数学公式块**（上游「+」面板） | 上游插入 | ✔ 在 |
| 我们的 callout 生活记录块（v1 实现） | 我们的插入 | ✗ 不在 |

结论：不是"强杀没落盘"（同会话的上游块都在），是**我们这条插入路径/节点本身的问题**。

### 三、根因（两个叠加的坑）

1. **一个事务里插多个节点 → 整批块动作发不到内核。**
   App 侧翻译 transaction 时（`editor_transaction_adapter.dart` 的 `InsertOperation.toBlockAction`）
   用 `prevId = getNodeAtPath(currentPath.previous)` 找兄弟前驱；若一个事务里连续插入两个节点，
   第二个节点的 previous 路径在**尚未更新**的文档里并不存在，`prevId` 变空串触发断言，
   于是整批动作没提交（编辑器已本地应用 → 界面照常渲染，重启后消失）。
   → 改为**一个事务只插一个节点**（与上游数学公式等实现一致）。

2. **属性表里有一个值不能被 JSON 编码 → 整张属性表被静默降级成 `{}`。**
   写入内核前，`document_data_pb_extension.dart` 的 `_dataAdapter` 会对 attributes 做 `jsonEncode`，
   异常被它 catch 后返回 `'{}'`（只写一条 Log.error）。上游 `calloutNode()` 把 `icon_type` 写成
   `FlowyIconType` **枚举对象**，正好踩坑 —— 也解释了为什么原生 callout 插入后图标总变成默认 📌。
   读取端 `CalloutBlockComponentBuilder.emoji` 用的是 `FlowyIconType.values.byName(type)`，
   说明**正确形态就是枚举名字符串**。
   → 我们的节点工厂改写 `icon.type.name`，并加 `assert(jsonEncode(attributes))` 自检，
   防止以后有人再塞枚举/自定义对象把 payload 一起坑掉。

### 四、修复后的真机验证（荣耀 ELZ-AN10 / Android 14）

| 验证项 | 结果 |
| --- | --- |
| 插入渲染（🌤️ + 日期 + 生活记录 + 心情/天气/地点） | ✔ |
| 退到后台 → 等 18s → 强杀 → 重启应用 → 重新打开同一篇日记 | ✔ **卡片仍在、渲染正确** |
| 数据层回读（collab 库全文检索） | ✔ `af_life_meta` / `af_date` / `af_mood` / `🌤️` 均落库 |

**结论：①「callout 承载 + af_life_meta payload + 覆写 callout 构建器」方案成立，
可安全作为后续所有"带数据块"的模板**（笔记里任何自定义数据块都按这个形态做：
标准块类型 + JSON-safe 属性 + 单节点单事务）。

**顺带产出的可复用经验**：

- 块插入必须"一个事务一个节点"；
- 块属性只放 JSON 原始值（字符串/数字/布尔/列表/普通 Map），枚举一律写 `.name`；
- 验证落库看 RocksDB，不看截图。

**遗留小问题（下一步顺手处理）**：生活记录卡片当前整体偏灰（chips 看着像禁用态），属于样式问题，
不影响数据；同期在 2026-09-23 日记里留了 2 个对照用空 callout，清理时一并删除。

## 2026-09-25 · 步骤⑬：日记增强（时间线 / 那年今日 / 心情统计）

**用户要求**：日历（日记模块）按 iOS18 风格做月历 + 每日日记 + 生活元数据之外，
再加时间线 / 那年今日 / 心情统计三块。

### 数据层（`app_diary_time` 包，全部用业务库 `diary_entries` 表）

| 能力 | 仓储方法 | SQL 要点 |
| --- | --- | --- |
| 时间线 | `recent(limit)` | `ORDER BY date_key DESC LIMIT ?` |
| 那年今日 | `onMonthDay(month, day)` | `substr(date_key, 6, 5) = 'MM-DD'`（天然跨年） |
| 心情统计 | `moodCounts(start, end)` | `GROUP BY mood` + 区间过滤 |

服务层对应 `timeline()` / `onThisDay(date)`（排除当天那篇）/ `moodCounts(month:)`。

### UI（日历标签页）

- 顶部新增 **iOS 风格分段控件「月历 / 时间线」**（AnimatedContainer + 圆角底槽，160ms 过渡）；
- **月历页**：月历 + 当天卡片 + 「心情统计 · 本月」（emoji + 占比条 + 天数，颜色与月历色点同源）；
- **时间线页**：心情统计 → 那年今日（同月同日历史日记）→ 时间线（最近 50 篇，倒序）；
  每行显示日期、周几、心情/天气/地点 chips，点击直接打开当天日记（没有文档则创建）。

**真机验证**（荣耀 ELZ-AN10）：分段切换正常；给 9-25 记心情 😄 后「心情统计」立刻出现
绿色进度条 + 「1 天」；时间线列出 09-25 / 09-24 / 09-23 / 09-11 / 09-10 并带周几；
点 09-24 直接打开日记正文（内容正确）✔

## 2026-09-25 · 步骤⑭：AI 模块 v0（本地 AIRepository + 摘要/标签）

**设计取向**：先保证「零配置、离线可用」，把「摘要 → 标签 → 记忆库」整条链路跑通，
大模型接入留成接口（蓝图：AI 可无限迭代）。

### 包内（`app_ai_ext`，全部纯 Dart、零网络）

| 组成 | 说明 |
| --- | --- |
| `ai_digests` 表 | 独立业务库（模块 `ai`，版本 1）：source_id / title / summary / tags / engine / updated_at |
| `AiDigestRepository` | `of` / `recent` / `upsert` / `remove`（标签用 `\u0001` 连接，避免与逗号冲突） |
| `TextDigester` 接口 | 插件点：`engine` + `digest(text)`；后续云端/本地 Ollama 各实现一个即可 |
| `LocalTextDigester` | **本地抽取式**：切句 → 中文 2~4-gram + 英文词频（带停用词）→ 句子打分取前 3 句 → 标签按 词频×长度 + 词边界 评分、重叠合并去碎片 |
| `AiSourceGateway` | 依赖倒置：正文读取由 App 侧实现（走内核 `DocumentService.getDocument` → 编辑器文档模型抽文本） |

### UI（底部导航「AI」标签改为「AI 记忆」）

- **本地摘要**卡：离线徽标 + 多行输入框 + 「生成摘要与标签」，生成后即时展示摘要与 `#标签`；
- **最近内容**：内核最近 10 篇文档，点 ✨ 一键对正文生成摘要；
- **AI 记忆库**：按时间倒序列出已生成记忆（标题 / 摘要 / 标签 / 时间 / 删除）。

**验证**：

- 算法脚本 `doc/tools/ai_digest_probe.dart`（模拟销售现场记录）：
  `tags=[客户现场, 不能超过, 报价单, 膜池]`，摘要把 3 句关键信息按原文顺序拼出；
- 真机：对 2026-09-23 日记点 ✨ → 记忆库出现该条摘要；对「功能测试」再生成一条；
- 数据层（`business.db` 的 `ai_digests`）确认落库：标题 / 摘要 / engine=local / 时间戳 ✔

## 2026-09-25 · 步骤⑮：WebDAV 快照同步（双库 + 附件、哈希校验、冲突人工选择）

### 快照内容（阶段一：全量）

`business/business.db`(+wal/shm) · `<工作区>/flowy-database.db` · `<工作区>/collab_db/**` · 附件目录
（`attachments`/`upload`/`images`，存在才打包）。打包用 `archive`，逐文件 sha256 写进 `manifest.json`。

### 关键设计（踩过的坑都固化在代码注释里）

1. **zip 条目时间戳固定**：否则每次打包哈希都变，"内容没变"永远判定不出来；
2. **同步判定用「内容哈希」= sha256(排序后的 路径+文件哈希)**，不用 zip 哈希
   （zip 里含 manifest.json 与打包时间）；zip 哈希只用于传输完整性校验；
3. **三方比较**：本地 / 远端 / 上次同步基线 → `upToDate` / `uploadLocal` / `downloadRemote` /
   `firstUpload` / **`conflict`**；冲突时弹框让用户在「保留本地 / 使用远端 / 取消」里选，**绝不自动覆盖**；
4. **恢复流程**：先下载 → 关业务库 → 逐文件校验哈希 → 归档旧文件到 `sync_restore_history/<时间戳>/`
   （**必须挪出快照范围**，否则残留文件会被下次快照打包，导致哈希永远收敛不了）→ 写入新文件 →
   自动生成 `sync_backup_<时间戳>.zip` → 提示"立即退出应用 / 稍后手动重启"；
5. 内核库是 RocksDB 目录 + SQLite，运行时无法原子冻结：所以恢复后必须重启（UI 明确提示）。

### 实现清单

| 文件 | 作用 |
| --- | --- |
| `packages/app_webdav_sync/lib/src/webdav_client.dart` | MKCOL / PUT / GET / HEAD / PROPFIND / DELETE 极简客户端（Basic 认证） |
| `.../src/snapshot.dart` | `SnapshotBuilder`（打包+清单+哈希）、`SnapshotRestorer`（校验+备份+改名替换） |
| `.../src/webdav_sync_service.dart` | 同步编排：`testConnection` / `plan` / `upload` / `download` / `downloadAndRestoreFromBytes` / `SyncStateStore` |
| `.../lib/app_webdav_sync.dart` | `webdav_config` 表（本机业务库存配置）+ `WebDavConfigRepository` |
| `lib/extensions/webdav_entry.dart` | App 侧装配：数据目录、设备名、快照前 WAL checkpoint、恢复前关库 |
| `lib/extensions/local_home/webdav_settings_page.dart` | 设置页：状态卡、配置对话框、测试连接、上传、检查并拉取、冲突对话框、恢复后重启提示 |

### 验证

`doc/tools/webdav_sync_probe.dart`（脚本内起最小 WebDAV 服务，跑真实同步服务）：

| 步骤 | 结果 |
| --- | --- |
| 1 连接 + 建远端目录 | ✔ |
| 2 首次判定 | `firstUpload` ✔ |
| 3 上传后再判定 | `upToDate` ✔（内容哈希收敛，验证了固定时间戳与内容哈希两处设计） |
| 4 仅本地变更 | `uploadLocal` ✔ |
| 5 双端都变更 | `conflict` ✔ |
| 6 选择"使用远端" | 恢复 3 个文件、正文变为远端版本、自动生成备份 zip ✔ |
| 7 恢复后再判定 | `upToDate` ✔ |
| 8 篡改压缩包一个字节 | 被哈希校验拦截 ✔ |

真机：设置 → WebDAV 快照同步页面正常打开；未配置时状态卡显示"未配置"、配置对话框字段齐全 ✔

**尚未做**（下一步）：真机对真实 WebDAV 服务器（如坚果云/群晖）跑一次上传下载；
附件目录若很大需要增量同步（阶段二）。

## 2026-09-25 · 步骤⑯：真块知识库双链（反向链接 / 出链索引）

**蓝图对应**：五大模块之三「真块知识库模块 —— 块引用、嵌入、双链、知识沉淀」。

### 先摸清上游能力边界

- 全仓库检索 `backlink / referencedPages / reverse link` → **上游没有任何反向链接实现**；
- 但上游**已内建引用能力的"素材"**：`@` 提及把引用写在文本属性里
  `mention = {type: 'page' | 'date' | 'link', page_id, block_id?}`
  —— `type == 'page'` 就是**页面引用**，带 `block_id` 时就是**块引用**。
- 因此本模块只做「**索引 + 呈现**」：把提及解析成出链写进独立业务库，再按目标页聚合成反向链接，
  **不改内核、不复制正文、不碰编辑器**。

### 代码结构

| 层 | 文件 | 说明 |
| --- | --- | --- |
| 扩展包（纯 Dart，零内核依赖） | `packages/app_kb_links/lib/src/ref_extractor.dart` | `extractPageRefsFromDelta()`：从 delta JSON 抽页面引用（纯函数，可脚本验证） |
| | `.../src/link_repository.dart` | 业务库两张表：`doc_links`（出链，PK = 来源+目标+块）与 `doc_link_index`（索引进度） |
| | `.../src/link_service.dart` | `indexDocument` / `backlinksOf` / `outgoingOf` / `stats` / `clear` |
| App 适配 | `lib/extensions/kb_links_entry.dart` | 读内核文档（`DocumentService.getDocument` → 遍历节点 → 喂给纯函数）；全库重建 |
| UI（文档内） | `lib/extensions/kb_links/backlinks_panel.dart` | 文档底部「🔗 反向链接 N · 出链 M」，点开显示引用方标题 + 所在段落片段，点击跳转 |
| UI（设置） | `lib/extensions/kb_links/kb_links_settings_page.dart` | 索引规模 + 一键重建（带进度） |
| 挂载点 | `lib/plugins/document/presentation/editor_page.dart` | 复用上游 `AppFlowyEditor(footer:)` 插槽追加面板（**不新增挂载点**） |

### 关键设计

1. **索引按需刷新**：打开/编辑一篇文档时只刷新**本篇**的出链（读一篇文档的成本），
   编辑停止 2 秒后自动再刷一次 —— 日常使用不需要全库扫描；
2. **全量重建**只在设置里手动触发（首次启用、恢复数据后补索引）；
3. **引用只存 id + 标题 + 段落片段**，正文仍在内核（蓝图原则）；
4. 自引用不入库；同一段里重复提及同一页只记一条；日期/链接类提及会被忽略；
5. 上下文片段会**剔除提及占位符**——否则"整段只有提及"时会显示一个没有信息量的 `$`。

### 验证

| 验证项 | 结果 |
| --- | --- |
| 纯函数脚本 `doc/tools/kb_links_probe.dart` | 页面提及去重 ✔、块引用（`block_id`）识别 ✔、日期/链接提及忽略 ✔ |
| 真机造真实引用（荣耀 ELZ-AN10） | 在「走出小巷」里 `@` 选 2026-09-24 → 面板立刻显示「反向链接 0 · **出链 1**」✔ |
| 反向链接侧 | 打开 2026-09-24 日记 → 面板显示「**反向链接 1**」，展开后是「走出小巷」并可直接跳回 ✔ |
| 数据层（`business.db`） | `doc_links` = `走出小巷 → 2026-09-24`（块 id 5TVvyO）✔；上下文已为干净空串 |
| 设置页重建 | 全库重建后 **已索引 35 篇 / 引用 1 条**，且原有引用被重新发现（说明是从内核真实数据解析出来的）✔ |

**下一步可做**：知识图谱视图（把 `doc_links` 可视化）、块引用的一键插入入口（上游"复制块链接"在移动端的可用性待验证）、
以及"未链接提及"（把文中纯文本的页面名一键变成引用）。

## 2026-09-25 · 步骤⑰：Windows 桌面端首次构建与可用性核查

**动因**：产品定位第一条是"安卓 + Windows 双平台"，而 Windows 端此前从未构建过。

### 构建

| 项 | 结果 |
| --- | --- |
| 工具链 | Visual Studio Community 2022 17.14 + Windows 10 SDK 10.0.26100、Rust 1.96 `x86_64-pc-windows-msvc`、cargo-make 0.37.18（`flutter doctor` 全绿） |
| 命令 | `flutter build windows --debug` |
| 结果 | ✔ 首次构建 770s，产物 `build\windows\x64\runner\Debug\AppFlowy.exe` |
| 启动 | ✔ 进程运行、主窗口 1086×920、`IsWindowVisible=true` |

### 可用性核查（数据层为准，本机会话无画面无法截图）

截图受环境限制（`CopyFromScreen` 与 `PrintWindow` 都拿不到 Flutter 画面，属会话渲染问题，不是应用问题），
因此改用**数据层证据**判断桌面端是否真的跑起来：

| 检查 | 结果 |
| --- | --- |
| 桌面数据目录 `%APPDATA%\io.appflowy\AppFlowy\data_dev\` | ✔ 本次运行新建（说明走的是**本地优先模式**，没有落到 `data_dev_beta.appflowy.cloud`） |
| 工作区与内核库 | ✔ 生成 `639081801330987008/`、`flowy-database.db`、`collab_db/`（RocksDB 正常打开与写入） |
| **我们扩展的落库** | ✔ `business/business.db` 里 `_biz_migrations = kb_links|1`，并建出 `doc_links` / `doc_link_index` —— 说明**桌面端文档页里的「反向链接」面板确实被实例化并跑通了**（sqlite3 原生库在 Windows 可用） |

### 结论与缺口（下一轮要做）

- ✔ 桌面端可以构建、启动、读写本地库，扩展包（含 sqlite3 原生库、archive/http 等）无平台差异问题；
- ⚠️ **桌面端目前仍是上游桌面 UI**：我们做的"抽屉 + 五标签首页（闪念/日历/CRM/AI/设置）"是**移动端外壳**
  （`LocalHomeShell`），桌面走的是上游侧边栏布局，所以**日记/CRM/AI 记忆/WebDAV 设置这些模块入口在桌面还摸不到**；
- 下一步建议：给桌面侧边栏加一组我们自己的入口（日记 / CRM / AI 记忆 / 同步设置），或让桌面也进本地优先外壳，
  再把 CRM 批量管理（大屏优势）挂上去。

## 2026-09-25 · 步骤⑱：桌面端模块入口 + 无画面 UI 校验工具

### 1) 桌面端也能进我们的模块了

把移动端已验证的页面改成可复用（`CalendarView` / `CrmView` / `AiMemoryView` 从私有改公开，
`AiMemoryView.containers` 变可选），新增桌面入口组件，**上游只插一行**：

| 文件 | 改动 |
| --- | --- |
| `lib/extensions/local_home/local_home_shell.dart` | 三个页面类公开化（移动端外壳继续用它们，行为不变） |
| `lib/extensions/desktop/local_modules_section.dart`（新增） | 「本地模块」入口区：日历·日记 / CRM 客户 / AI 记忆 / 知识库双链 / 快照同步 |
| `lib/workspace/presentation/home/menu/sidebar/sidebar.dart` | 侧边栏"新建页面"按钮下方插入 `LocalModulesSection(userProfile: ...)`（+2 行含 import） |

入口只是路由，业务逻辑全部复用既有页面（不复制代码）。

### 2) 新增「无画面 UI 校验」工具（本机 Windows 会话抓不到 Flutter 画面）

`doc/tools/dump_widget_tree.dart`：连 `flutter run` 的 Dart VM Service，
调 `ext.flutter.debugDumpApp` 把控件树拉下来并按关键字检索。

用法：
```
flutter run -d windows --debug          # 记下日志里的 VM Service URI
dart doc/tools/dump_widget_tree.dart http://127.0.0.1:9052/TOKEN=/ LocalModulesSection
```

### 3) 本次验证（Windows 真机 = 本机）

| 验证项 | 结果 |
| --- | --- |
| `flutter build windows --debug` | ✔ 构建通过（增量 78s） |
| `flutter run -d windows` 启动 | ✔ 本地模式（`authenticator: local`）、六个扩展包全部加载成功（日志含 `app_kb_links`） |
| 控件树检索 `LocalModulesSection` | ✔ 命中（第 7483 行 / 共 8441 行） |
| 控件树检索 `本地模块` `CRM 客户` `AI 记忆` `知识库双链` `快照同步` | ✔ 全部命中，说明入口确实完成布局渲染 |
| 安卓端回归 | ✔ APK 重新构建 + 安装通过（类公开化没有影响移动端外壳） |

**已知限制**：桌面端目前是"上游桌面 UI + 我们的入口与文档级能力"，
移动端那套"抽屉 + 五标签"外壳仍只在手机上生效；桌面 UI 是否要统一成移动端风格，待产品决策。

## 2026-09-25 · 步骤⑲：桌面侧栏入口样式对齐 + 双链增强（关系图谱 / 未链接提及）

### 1) 侧栏「本地模块」样式修正（用户反馈"与原有元素不统一"）

**根因**：侧栏外层 `Column` 的 `crossAxisAlignment` 是 `center`，我们的组件没有撑满宽度，
于是整块被居中 —— 看起来像"缩进了一大截、字号还更小"。

**修正**：

| 项 | 之前 | 现在 |
| --- | --- | --- |
| 宽度 | 未约束（被父级居中） | `SizedBox(width: double.infinity)` |
| 行样式 | 自造 `FlowyButton`（图标 16、字号 13、default 内边距） | 完全复用上游「新页面」行：`Container(padding: 8, height: HomeSizes.newPageSectionHeight)` + `FlowyButton(leftIcon: FlowySvg(16x), leftIconSize: 20, margin: left 4, iconPadding: 8)` + `FlowyText.regular(lineHeight: 1.15)` |
| 图标 | Material 线性图标 | 上游 SVG 图标体系：`calendar_s` / `person_s` / `ai_sparks_s` / `link_to_page_s` / `settings_sync_m` |
| 小节标题 | 自造样式 | 上游写法 `FlowyText('本地模块', fontSize: 12, color: hintColor)` |

**量化验收**（无画面环境：拉渲染树比对文本 x 偏移与字号，工具见 `doc/tools/dump_widget_tree.dart --render`）：

| 元素 | 文本 x 偏移 | 字号 |
| --- | --- | --- |
| 上游「搜索」 | 28.0 | — |
| 上游「新页面」 | 32.0（前面多一个 24px 图标） | 14.0 |
| 我们的「日历 · 日记」 | **28.0**（与「搜索」对齐） | **14.0**（与新页面一致） |
| 我们的「本地模块」标题 | 16.0 | 12.0 |

### 2) 双链增强（用户同意的"第二条"）

#### 2.1 关系图谱（`lib/extensions/kb_links/knowledge_graph_page.dart`）

- 数据：`LinkService.graphData()` —— 节点（页面）+ 边（引用）+ 被引用/出链计数（新加了 `LinkRepository.all()`）；
- 布局：**自研极简力导向**（两两斥力 + 边弹簧 + 向心 + 阻尼，固定随机种子保证同一份数据图形稳定）；
  节点 ≤ 4 个时改用**环形排布**（力导向会把 2 个节点推到对角，很难看）；
- 绘制：`CustomPainter` 画边/节点/标签，节点半径随被引用次数增长；
- 交互：点节点高亮并跳转到该页面；图下方给「被引用最多」榜单（图很小时照样有信息量）；
- 入口：设置 → 知识库双链 → **打开关系图谱**；若索引为空会先自动跑一次全库索引。

**真机验证**：索引 36 篇 / 引用 1 条 → 图谱显示「2 个页面 · 1 条引用」，
一条边连接「走出小巷」与「2026-09-24」，榜单显示「2026-09-24 · 1 处引用」✔

#### 2.2 未链接提及（正文写了页面名 → 一键变成引用）

- 纯函数放扩展包：`findUnlinkedMentionsInNodes()`（`ref_extractor.dart`）—— 只匹配其它页面标题（≥2 字）、
  跳过已引用区间、同一目标页只提示一次、先长后短、同一片段不被更短标题重复命中；
- App 侧 `findUnlinkedMentions()` 只做"编辑器模型 ↔ 纯数据"转换；
- 文档底部双链面板新增「可以链接（正文提到但还没引用）」区块：点一下用
  `transaction.formatText(node, start, length, buildMentionPageAttributes(...))` 把那段文字变成真实引用，
  之后面板自动刷新（出链 +1，对方反链 +1）。

**验证**：`doc/tools/kb_links_probe.dart` 新增用例 —— 同一段里"客户拜访记录"与独立的"客户"
各命中一次、已是指用的"报价单要点"不提示、单字标题被最短长度规则排除、自引用被排除 ✔
（真机端到端待可稳定输入中文后再补：adb 注入文本会被输入法吞进候选栏）

## 2026-09-25 · 步骤⑳：结构按产品决策收敛为「4 个分类容器 + 子页面」

**产品决策（用户拍板）**：沿用上游"页面即容器、可无限嵌套"的概念，只固定创建 4 个分类容器
——**笔记 / 日历 / CRM / AI 交流**；「闪念 / 工作记录 / 生活日记」一律作为**笔记容器下的普通子页面**；
移动端继续用底栏五标签，直接链接到对应分类页面。

### 关键事实（决定了改动很小）

- 容器**本来就是普通顶层页面**（`WorkspaceService.createView(viewSection: Public)`，与「新页面」同一路径），
  只在 `view.extra` 里写了 `af_container/af_module/af_icon`；
- 所以"改结构"= 改页面树位置 + 改 extra 标记，**没有任何新表/新层级**；
- 内核现成 API 足够：`ViewBackendService.moveViewV2(newParentId)` 改父节点、`updateView(name/extra)` 改标记。

### 改动清单

| 层 | 文件 | 变化 |
| --- | --- | --- |
| 模型 | `packages/app_containers/lib/app_containers.dart` | 默认容器改为 **笔记 / 日历 / CRM / AI 交流**（新增 `diary` 模块）；新增 `kDefaultSubPages`（笔记下 闪念 / 工作记录 / 生活日记）与 `kDiaryParentPageName`；`flash_note` 保留为历史模块值 |
| 适配器 | `lib/extensions/adapters/container_repository_impl.dart` | 新增 `alignStructure()`（幂等，启动时一次）：① 旧「闪念」容器**降级**为笔记下的子页面（`moveViewV2` + 去掉容器标记，**正文与子页面完全不动**）② 补齐笔记下的默认子页面 ③ 把散落在笔记根部的日记页（标题 `yyyy-MM-dd`）收进「生活日记」 |
| 日记 | `lib/extensions/diary_entry.dart` | 日记文档落点从「笔记」根改为 **笔记 → 生活日记**（父页面不存在会自动建，进程内缓存 id） |
| 外壳 | `lib/extensions/local_home/local_home_shell.dart` | 首页＝**笔记**分类容器；日历 / CRM / AI 三个标签＝对应**分类容器页面**，各自顶栏带一个专属功能入口（月历 / 客户卡 / AI 记忆）；抽屉容器列表仍是 4 预设 + 自定义 |
| 文档页 | `lib/extensions/local_home/child_pages_section.dart`（新增）+ `editor_page.dart` | 文档底部新增「**子页面 N**」区块：列出本页子页面 + 一键「新建子页面」，让手机端也能像文件夹一样上下钻取（挂在同一个 `footer:` 插槽，不改编辑器内部） |

### 真机验证（荣耀 ELZ-AN10）

| 验证项 | 结果 |
| --- | --- |
| 启动迁移 | ✔ 首页标题变为「笔记」，子页面出现 **工作记录 / 闪念 / 生活日记**；旧「闪念」容器内容随之保留 |
| 日记归档 | ✔ 「生活日记」下已有 5 篇日期页（2026-09-25 / 09-11 / 09-23 / 09-24 / 09-10） |
| 子页面区块 | ✔ 打开「生活日记」底部显示「子页面 5」，可点进、可新建 |
| 抽屉容器列表 | ✔ 只剩 **笔记 / 日历 / CRM / AI + 自定义**（不再有 闪念 容器） |
| 底栏分类页 | ✔ 日历＝日历容器页（顶栏📅月历入口）、AI 交流＝AI 容器页（顶栏✨AI 记忆入口）、CRM 同理 |

**说明**：这次只动"页面树结构 + extra 标记 + 新建落点"，**没有删除任何页面或正文**；
恢复旧结构也只需反向 `moveViewV2` + 还原 extra。

## 2026-09-25 · 步骤㉑：标签系统 v1 + 统一时间线 v1（原生 Grid 数据库）

**产品决策**：① 采用"树为主 + 标签为辅"；② 时间线采用混合方案（原生 Grid 作主时间轴 + 日记保留现状 +
文档内时间块留作后续）。本轮先落地 ① 与 ② 的第一阶段。

### 1) 标签（树为主、标签为辅）

| 项 | 实现 |
| --- | --- |
| 存储 | 页面自己的 `view.extra`，键 `af_tags`（**不新增表、不改内核**，随内核同步/回收站/快照走） |
| 读写 | `lib/extensions/page_tags.dart`：`tagsOfView` / `loadPageTags` / `savePageTags`（**合并**原有 extra 其它键，不破坏容器标记与图标） |
| UI | 文档底部「标签」区块（`PageTagsSection`）：显示 `#标签` chips、可添加/删除；挂在同一个 `footer:` 插槽 |

**待补**：列表页按标签过滤的 chips 行（下一轮）。

### 2) 统一时间线（原生 Grid 数据库 = 主时间轴）

| 组成 | 说明 |
| --- | --- |
| 数据库页 | 启动时幂等创建「**时间线**」Grid 页，挂在「日历」分类容器下 |
| 字段 | 日期（DateTime，原生日历视图必需）、类型（文本）、来源（文本，写页面 id）；行标题带类型前缀（`日记 · 2026-09-25`），保证 Grid/看板/日历卡片都能看到分类 |
| 写入桥 | `lib/extensions/timeline_entry.dart` 的 `recordTimelineEvent(date/kind/title/sourceViewId)` —— **各模块统一调用**：`TimelineKind = 闪念/日记/工作记录/CRM/AI 交流/笔记` |
| 已接入写入点 | ① 日记创建（`diary_entry.dart`）② 分类容器内新建记录（`_ContainerRecordsView`）③ CRM 新建客户 |
| 历史回填 | 启动时把业务库 `diary_entries` 的日记一次性回填成事件行（用业务库 `timeline_backfill` 表打标记，只跑一次） |
| 入口 | 日历分类页顶栏「📈 时间轴」→ 打开原生 Grid 页；用户可在该页右上角「+」自行添加 **Calendar / 看板** 视图（上游原生能力） |

**真机验证**（荣耀 ELZ-AN10）：

| 验证项 | 结果 |
| --- | --- |
| 「时间线」页自动创建在日历容器下 | ✔（日历分类页可见「时间线」子页面） |
| 历史日记回填 | ✔ Grid 里出现 2026-09-10 / 09-11 / 09-23 / 09-24 / 09-25 五行 |
| 新内容自动进时间轴 | ✔ 在笔记容器新建记录后，Grid 自动追加一行「笔记」 |
| 手机端原生 Grid 可用 | ✔（上游 `MobileGridScreen`，可直接编辑行/单元格） |

**已知问题（下一轮修）**：
1. 非主字段的文本列（类型/来源）通过 `createRow(withCells:)` 写入后**单元格为空**——
   主字段（行标题）正常写入；已改为"标题带类型前缀"保证可见，列写入需要再查内核 cell changeset 约定；
2. Grid 里出现 3 个空行（疑似建库/早期写入残留），需加一次"清理空行"；
3. 标签过滤条、文档内时间块（`af_when` + 索引器）尚未做。

## 2026-09-25 · 步骤㉒：清空两端数据做"全新安装"回归

**目的**：把手机与电脑的应用数据清空，从零跑一遍，确认所有结构都能自动重建、状态正常。

### 清空方式（都可回滚）

| 端 | 操作 | 备份 |
| --- | --- | --- |
| 手机 | `adb shell pm clear io.appflowy.appflowy` | 先 `adb exec-out run-as ... tar -cf - files` 导出到 `E:\Dev\AppFlowy\.backup-20260925-101224\android-files.tar`（79MB） |
| 电脑 | 停应用后把 `%APPDATA%\io.appflowy\AppFlowy\data_dev` 改名留档 | `data_dev.bak2-20260925-101325`（旧云端目录 `data_dev_beta.appflowy.cloud` 未动） |

### 发现并修掉的真问题

**结构对齐原先只挂在移动端外壳（`LocalHomeShell`）上，桌面端全新安装不会创建分类容器**。
改为在桌面侧栏组件 `LocalModulesSection` 里也触发一次（幂等、失败只记日志）。

### 全新安装验证结果

| 端 | 验证项 | 结果 |
| --- | --- | --- |
| 手机 | 首页＝「笔记」，子页面 工作记录 / 闪念 / 生活日记 全部自动创建 | ✔ |
| 手机 | 「时间线」Grid 自动创建在日历容器下 | ✔ |
| 手机 | 新建记录 → 时间线自动追加一行（标题=类型「笔记」） | ✔ |
| 手机 | 业务库表 | ✔ `diary_entries / timeline_backfill / doc_links / doc_link_index`，迁移记录 `diary=1,2 / kb_links=1` |
| 手机 | 时间线里的空行数量 | 3 行 —— **上游新建 Grid 数据库的默认行为**（全新库、我们未写任何数据时就是 3 个空行），不是数据残留 |
| 电脑 | 全新工作区 `639613376594448384`、新 `business.db`、collab 库 | ✔ |
| 电脑 | RocksDB 实测：`笔记 / 日历 / CRM / AI 交流 / 生活日记 / 时间线` 全部已创建 | ✔（用 `doc/tools/inspect_collab_db.py` 检索确认） |

**结论**：两端从零启动都能自动重建完整结构，没有依赖历史数据的隐患；
时间线的空行问题已定性为上游默认，不需要清理（用户可自行删除）。

## 2026-09-25 · 步骤㉓：手机端按"方向 A"重构（UI 参照 `E:\Dev\moodiaryCRM`）

**决策**：手机端从"目录树"改为**内容流**（方向 A），视觉与交互语言参照 moodiaryCRM 的手机端。

### 参考项目提取出的设计语言（已落到我们的代码里）

| 维度 | 参考做法 | 我们的落地 |
| --- | --- | --- |
| 圆角 | 8 / 12 / 16 | `Mob.radiusSmall/Medium/Large`（`mobile_ui_kit.dart`） |
| 列表卡片 | `Card.filled` + 圆角 12 + 12px 内边距 + 元信息行 | `MobCard` + 记录卡片（图标/标题/时间/分类/标签） |
| 底栏 | 高 56 + 安全区、图标 24 + 下方标签、顶边 0.5px 细线、选中加粗 | `MobBottomBar`（替换上游 `NavigationBar`） |
| FAB | 56×56 圆角 16、点击速记、**上滑展开菜单**、展开时旋转 135° | `MobRecordsFab`（点击＝新建记录并直接进编辑器；上滑＝展开"回到顶部/新建日记"） |
| 筛选 | 底部弹层多选 + 清除/应用 | 记录流右上「筛选」→ 按标签多选（读页面 `af_tags`） |

### 手机端结构变化

| 之前 | 现在 |
| --- | --- |
| 首页＝当前容器的记录页（要先选容器） | **首页＝记录流**：跨分类按时间倒序的卡片列表 |
| 分类＝要"点进去"的层级 | 分类＝**顶部筛选 chips**（全部 / 闪念 / 工作记录 / 日记 / CRM / AI 交流 / 标签） |
| FAB 落在容器根，想记闪念还得进两层 | **FAB 一步新建**，落点＝当前筛选对应的位置（"全部"时落「笔记」） |
| 日历/CRM/AI tab＝容器页 + 顶栏再进功能 | 三个 tab **直接是功能页**（月历 / 客户卡 / AI 记忆） |
| 底栏＝上游 `NavigationBar` | `MobBottomBar`（对齐参考项目的 56px 细边框风格） |

抽屉保留"按分类看记录"的另一条路径：点分类 → 打开该分类的记录视图（列表/网格/时间线三选一）。

### 真机验证（荣耀 ELZ-AN10）

| 验证项 | 结果 |
| --- | --- |
| 首页记录流 | ✔ 卡片式列表、分类 chips、「全部/笔记」筛选 |
| 分类壳过滤 | ✔ 闪念/工作记录/生活日记这类"归档节点"不再混进记录流 |
| FAB 一步新建 | ✔ 点击后直接进入编辑器（新页面，底部已有子页面/标签/反向链接区块） |
| tab 直连功能页 | ✔ CRM tab 直接显示客户卡（含"新建客户"） |
| 底栏样式 | ✔ 56px + 细上边框 + 图标/标签 + 选中加粗 |

**尚未做（下一轮）**：
1. 文档底部三块（子页面 / 标签 / 反向链接）收成**一行折叠条**；
2. FAB **上滑展开菜单**的手势在真机上还没验证通过（adb 模拟手势会漏到父级，需要手动确认手感）；
3. 记录卡片目前只有标题+时间+分类，**没有正文预览**（要看正文得读文档，属于下一步优化）；
4. 首页搜索入口（`onOpenSearch`）还没接。

## 2026-09-25 · 步骤㉔：UI 迁移第 1 轮（配色 + 视图模式）

**背景**：用户觉得"还不够彻底"，希望把 `E:\Dev\moodiaryCRM` 的手机端 UI 直接迁移过来。

### 可行性结论（先给判断）

**不能整包原样搬，但可以按层搬。** 三个硬差异：

| 差异 | 参考项目 moodiaryCRM | 本项目 AppFlowy | 处理方式 |
| --- | --- | --- | --- |
| 状态管理 | GetX（`Get.put` / `.obs` / `Obx`） | Bloc / Provider | **照抄组件与布局，不引入 GetX**（两套状态体系并存会失控） |
| 数据源 | Isar 本地对象（Diary/Block 直接给卡片用） | 内核页面树 + 文档 CRDT | 加**视图模型适配层**（ViewPB → 卡片字段），正文预览需要读文档（异步/索引） |
| 主题与依赖 | 自有 colorScheme（6 套传统色 + PANTONE）、font_awesome、lottie、audio | AppFlowyTheme + 已有依赖 | 配色可用 `ColorScheme.fromSeed` 覆盖；图标/动画按需引入 |

### 三轮计划

| 轮次 | 内容 | 状态 |
| --- | --- | --- |
| 第 1 轮 | **配色**（6 套传统色 + PANTONE 摩卡，可切换、随会话持久化）+ 记录流**列表/网格**两种卡片（网格版按参考项目布局）+ 底栏/FAB 已在上一轮对齐 | ✔ 本轮完成 |
| 第 2 轮 | 分类侧栏（nav_sidebar）、视图模式（列表/网格/块/日历）、搜索弹层、iOS 风格月历页 | 待做 |
| 第 3 轮 | 设置页分组视觉、空态/加载/Toast 样式统一、细节手感打磨 | 待做 |

**明确不搬**：编辑页（保留 AppFlowy 块编辑器 —— 这是本项目的核心资产）、Isar/GetX、同步页（我们已有 WebDAV）。

### 本轮落地

- `lib/extensions/local_home/mobile_theme.dart`：`MobPalette`（百草霜/群青/青黛/水朱华/芰荷/缃叶/摩卡慕斯）+
  `MobThemeController`（`shared_preferences` 持久化）+ `MobTheme`（给手机端外壳套 `ColorScheme.fromSeed`）+
  设置页「主题色」弹层（含"跟随系统"）。
- `records_feed_page.dart`：顶栏新增**列表/网格**切换；网格卡片按参考项目的
  `GirdDiaryCardComponent` 布局（顶部类型行 + 4 行标题 + 底部时间 + 标签）。

真机验证：记录流列表视图正常、顶栏出现筛选/网格两个图标 ✔（网格视图的点击切换在 adb 上坐标命中不稳定，需手动确认）

## 2026-09-25 · 步骤㉕：UI 迁移第 2 轮 —— 自建 iOS 风格日历

**用户决策**：编辑页保留 AppFlowy 块编辑器；**不搬**参考项目的媒体页 / 块视图 / iOS 月历页，
而是**新建一个符合本项目架构的 iOS 风格日历**。

### 新增 `lib/extensions/local_home/ios_calendar_page.dart`

数据全部来自我们自己的三层，没有引入任何新数据模型：

| 区域 | 数据来源 |
| --- | --- |
| 月历圆点（有内容标记） | 日记业务库（心情色）+ 内核页面树（当天有没有记录） |
| 当天卡片：心情 / 天气 / 地点 | 日记业务库 `diary_entries` |
| 当天记录列表 | 内核页面树（按最后编辑/创建日期归类，排除容器与"分类壳"） |
| 时间线 / 那年今日 / 心情统计 | 已有的 `DiaryService` + 时间线数据库（时间轴入口打开原生 Grid） |

### iOS 风格要素

- 顶栏：标题 + 「今天」+ 时间轴入口；
- 月历/时间线**分段控件**（圆角底槽 + 白色滑块 + 阴影）；
- **大标题月份**（`headlineSmall`, w600）+ 左右切换 + 月份切换 160ms 过渡；
- 日期格：圆形选中/今天态、下方内容圆点（颜色 = 当天心情色，无心情时用主题色）；
- 当天区块：`MobCard` 圆角卡片 + chips + 主按钮 + 当天记录列表（带时间）；
- 底部：心情统计（emoji + 进度条 + 天数）。

### 真机验证（荣耀 ELZ-AN10）

| 验证项 | 结果 |
| --- | --- |
| 月历渲染 + 今天/选中态 | ✔ 25 号高亮，22 号因有内容显示圆点 |
| 当天记录聚合 | ✔ 「2026年9月25日 周五」下列出 3 条当天记录（含时间） |
| 心情/天气/地点 chips | ✔ 可点开弹层记录 |
| 写日记入口 | ✔「写今天的日记」/「打开当天日记」按存在与否切换 |
| 时间轴入口 | ✔ 顶栏图标直接打开「时间线」原生数据库页 |

**说明**：原 `CalendarView`（上一版月历页）保留在代码里未删除，便于对比回退；
手机端「日历」标签现在用新的 `IosCalendarPage`。

## 2026-09-25 · 步骤㉖：第 3 轮打磨 + 日历增强 + 内容时间（三条一起做）

### ① 第 3 轮打磨

| 项 | 实现 | 状态 |
| --- | --- | --- |
| 文档底部三块 → **一行折叠条** | 新增 `LocalHome/doc_footer.dart`：折叠时显示 `📁 N  #M  🔗 K`，展开才渲染「子页面 / 标签 / 双链」三块；右侧多一个「时间标记」按钮 | ✔ 代码就位（真机逐项点验待补） |
| 记录卡片**正文预览** | 记录流懒加载摘要：每批 3 篇、每轮上限 40 篇，读内核文档取前 80 字并缓存，列表/网格两种卡片都显示 | ✔ 真机确认（卡片显示"地方其实就是已经可以输入主要内容了/"等摘要） |
| 首页**搜索** | 顶栏搜索图标切换为输入框，按 标题 / 正文摘要 / 分类 / 标签 匹配 | ✔ 入口与过滤已就位 |
| 主题色即时预览 | `MobTheme` + `ValueNotifier`，设置页换色立即生效（已持久化） | ✔ 上一轮已具备 |

### ② 日历增强（节假日 + 农历位）

- 新增 `extensions/calendar_china_days.dart`：把参考项目 `china_holidays_2026.json` 的
  **2026 全年放假/调休**内联成常量表（含元旦、春节、清明、劳动节、端午、中秋、国庆与各补班日）；
- 月历格：节假日数字标红 + 下方「休」，调休日标灰 + 下方「班」，与内容圆点不冲突（有节假日标记时优先显示标记）；
- 当天卡片标题带上节日名（例如「2026年9月25日 周五 · 中秋节」）。

**真机确认**：2026-09-25/26/27 显示橙色「休」，2026-09-20 显示灰色「班」，22 号仍显示内容圆点 ✔

### ③ 内容时间（`af_when` 时间标记）——让日历按"内容时间"归类

| 层 | 实现 |
| --- | --- |
| 块形态 | **标准 callout + `af_when` payload**（与「生活记录」同一套安全形态：标准块类型 + JSON 安全属性）|
| 渲染 | 覆写 callout 构建器：带 `af_when` 的节点渲染成**一行紧凑时间标签**（🕒 日期 + "内容时间（日历按它归类）"），点它可以改日期 |
| 插入 | 文档底部折叠条右侧「时间标记」按钮 → 单节点单事务插入当天日期的标记块 |
| 索引 | 文档底部加载时解析本篇所有 `af_when` → 写业务库 `when_index`（`app_diary_time` 迁移 v3 新增该表）|
| 日历归类 | 日历加载时先读 `when_index`：**有内容时间的页面按内容时间落到那天**，其余页面才回落到"最后编辑时间" |

**意义**：今天补记昨天的事，会正确落在昨天 —— 这是"日历/时间线贯穿全项目"的最后一块拼图。

**待真机验证**：插入时间标记 → 重启 → 日历上该记录出现在标记日（下一轮补）。

### 未做/已知

- **周视图**：本轮没做（月历 + 时间线 + 当天记录已覆盖主要场景），保留为后续选项；
- 农历目前只做了"放假/调休 + 节日名"，没有做初一/十五这类**农历日**（需要引入农历库或内置换算表）。

## 2026-09-25 · 步骤㉗：记录页线性滑动标签 + 日历周视图 + 农历

### ① 记录页 TABS 改为「线性滑动标签」

原来的分类 chips 换成 **TabBar 风格标签栏**：

- 标签内容：`全部 N / 日记 / 笔记 / 闪念 …`（按现有数据自动生成，横向可滚动）；
- **细下划线指示器**（`indicatorSize: label`、`indicatorWeight: 2.5`、无 divider），
  点击切换与**左右滑动切页**时下划线都跟着滑动（`TabBar` + `TabBarView` 原生联动）；
- 页脚改用显式 `TabController`：滑动切页后能知道当前分类，**FAB 新建的落点跟着当前分类走**；
- 标签（`#tag`）筛选移到右上「筛选」弹层，不再和分类混在一排。

真机确认：顶栏显示「全部 6 | 日记 | 笔记」，下划线跟随选中项 ✔

### ② 日历：新增周视图

分段控件从两档变三档：**月历 / 周历 / 时间线**。

- 周历显示「x月x日 - x月x日」+ 7 天条（带农历日、休/班标记、内容圆点），‹ › 按周翻；
- 下方仍是当天卡片（心情/天气/地点 + 打开日记 + 当天记录）。

真机确认：周历显示 9/21-9/27 七天，当天卡片「2026年9月25日 周五 · 中秋节」✔

### ③ 农历 / 节气 / 节日

- 引入 `lunar` 包（6tail，纯 Dart、无原生依赖）；
- 新增 `extensions/calendar_lunar.dart`：`lunarInfoOf(date)` 返回 农历日 / 农历月 / 节气 / 节日；
- 日历格小字优先级：**放假(休)/调休(班) > 农历节日或节气 > 农历日**（初一/十五用主题色加重）；
- 当天卡片多一行「农历八月十五 · 中秋节」。

真机确认：7 号「白露」、10 号「教师节」、23 号「秋分」、每格农历日（二十/廿一…初一…）全部正常 ✔

### 仍待真机确认（下一轮第一件事）

1. **`af_when` 内容时间**端到端：页面底部「时间标记」→ 选一个非今天的日期 → 日历上该记录应出现在标记日；
2. 文档底部**折叠条**的展开/收起手感；
3. FAB **上滑展开菜单**手势（adb 模拟会漏到父级，需手动点）。

## 2026-09-25 · 步骤㉘：标签左对齐 / CRM 同款标签 / 时间标记修复 / 排序

### ① 记录页标签改为**左对齐**（自绘 `MobSlidingTabs`）

根因：Material 3 的滚动型 `TabBar` 自带起始偏移，视觉上像居中，且样式受主题影响。
改为自绘组件 `extensions/local_home/mob_sliding_tabs.dart`：

- **左对齐**、可按内容宽度横向滚动、每格点击区 = 文字宽 + 左右 14px；
- 下划线（2.5px）长度对齐**文字**（不是整格），监听 `TabController.animation`，
  因此点击切换与**左右滑动切页**时下划线都连续跟随；
- 切换标签时自动把当前标签滚进可视区。

真机确认：`全部 6 | 日记 | 笔记` 贴左显示，下划线在文字下方 ✔

### ② CRM 页阶段筛选使用同一套标签

把原来的 ChoiceChip 行换成 `MobSlidingTabs`（`全部 / 线索 / 接触 / 方案 / 投标 / 成交 / 流失`），
用 `TabController` 驱动过滤并复用同一个 `_stageFilter` 逻辑。

真机确认：CRM 顶部贴左显示 7 个阶段标签 + 下划线 ✔

### ③ 修复「时间标记」点击有时无反应

原因：旧实现要求"编辑器当前有折叠选区"，而底部工具栏点击时编辑器常常已失焦 → 直接 return。

现在改为：

1. **先弹日期选择器**（`showDatePicker`，默认今天），用户明确要标哪一天；
2. 有光标 → 插到光标所在块之后；**没有光标 → 插到文档末尾**，并把光标落到该块；
3. 渲染上仍是一行可点的紧凑时间标签（点它可以再改日期）。

### ④ 排序：默认按创建时间 + 「更多」菜单

- **默认排序改为「创建时间 ↓」**：打开/编辑/关闭页面不再改变记录位置（之前用 `lastEdited`，一进一出顺序就变）；
- 新增排序模式：创建时间 ↓ / 创建时间 ↑ / 编辑时间 ↓ / 标题 A→Z；
- 顶栏三个按钮（搜索 / 筛选 / 网格）收成 **搜索 + 「⋮ 更多」**：
  更多里包含「排序（单选）」「切换列表/网格视图」「按标签筛选」，并在底部说明默认排序规则；
- 顶栏「⋮」在筛选或非默认排序时会高亮（提示"有非默认状态"）。

### 仍待真机手动确认

1. `af_when` 内容时间端到端：插入时间标记（现在会先弹日期）→ 选非今天 → 日历落在标记日；
2. FAB 上滑展开菜单手势。

## 2026-09-25 · 步骤㉙：偏好持久化 + 卡片长按操作 + CRM 客户详情

### ① 偏好持久化（`mob_prefs.dart`）

- 记录页的**排序方式**与**列表/网格视图**存 `shared_preferences`，重启后恢复；
- 默认仍是「创建时间 ↓ + 列表」；
- 集中在一个文件里，后续加偏好（日历默认视图、折叠条默认展开等）都往这里加。

### ② 记录卡片**长按操作**

长按任意记录卡片弹出操作表（真机确认）：

| 操作 | 实现 |
| --- | --- |
| 重命名 | `ViewBackendService.updateView(name:)` |
| 移动到… | 列出「分类容器 + 它们的子页面」作为候选目标，用 `moveViewV2` 改父节点 |
| 打标签 | 空格/逗号分隔，写入页面 `extra` 的 `af_tags`（与文档底部标签区同源） |
| 删除 | 二次确认后 `deleteView`（进回收站，可恢复） |

### ③ CRM 客户详情页（`crm_customer_detail_page.dart`）

点客户卡片进入详情（之前点了没反应）：

- **档案编辑**：姓名 / 公司 / 负责人 / 电话（点击弹输入框即时保存）；
- **阶段**：线索 / 接触 / 方案 / 投标 / 成交 / 流失，点一下即改；
- **跟进记录**：一键写一条跟进 → 追加到客户备注流水，**同时写入统一时间轴**（kind=CRM），
  于是客户动态会出现在日历/时间线里；
- **档案信息**：创建/更新时间；右上角可删除客户（二次确认）。

### 仍待手动确认

- CRM 详情页需要先**新建一个客户**再点开（清库后客户列表为空，adb 无法可靠输入中文）；
- 偏好持久化的效果：切换排序/视图 → 杀进程重开 → 应保持上次选择。

## 2026-09-25 · 步骤㉚：CRM 升级为「线索 → 项目 → 合同 → 收款」实体链

**用户要求**：CRM 里分「客户 / 联系人 / 线索 / 合同」等页面；**阶段属于"线索转项目之后"**；
以**项目**为中心关联客户、联系人、线索、合同、应收与回款、发票；客户与联系人互相关联；
线索可转项目；字段与表结构要能自定义扩展；CRM 的文本内容与首页记录关联；CRM 事件带时间并与日历关联。

### 数据模型（`app_crm_biz` 迁移 v2，全部在独立业务库）

| 表 | 作用 |
| --- | --- |
| `crm_entities` | **通用实体表**：线索/客户/联系人/项目/合同/收款共用。核心字段固定列（title/stage/amount/phone/owner/note/event_time），另含 `customer_id / contact_id / project_id / lead_id` 建立实体关系链，**扩展字段放 `extra`(JSON)** |
| `crm_events` | 跟踪记录：类型（电话/拜访/会议/微信/邮件/备注/阶段/转项目）+ 内容 + **时间** + 关联记录页 id |
| `crm_links` | 实体 ↔ 内核记录页（CRM 内容与首页记录打通） |
| `crm_field_defs` | **自定义字段定义**：想加字段不用改表结构（值落在实体 `extra`） |

阶段（可继续扩展）：线索 `新线索/已联系/已确认/已转项目/已丢弃`；
项目 `需求调研/方案编制/报价/投标/商务谈判/已成交/已流失`；
合同 `草稿/已签署/履行中/已完成/已终止`；收款 `未收/部分收款/已收齐/逾期`。

### UI（手机端 CRM）

- 顶部**六个分类标签**（与记录页同款线性滑动标签）：线索 / 客户 / 联系人 / 项目 / 合同 / 收款；
- 每类：卡片列表（标题 + 阶段 + 金额 + 关键日期 + 更新时间）+ FAB 新建（字段按类型裁剪）+ 点开详情；
- **详情页**：核心字段逐项编辑、阶段 chips 流转、**关联记录**（挑一篇内核页面挂上来）、
  **自定义字段**填写、**跟踪记录**（类型 + 内容 + 时间 + 「同时生成一篇记录」）；
- **线索 → 转项目**：一键生成项目实体（继承客户/联系人/备注），线索阶段置「已转项目」；
- **字段设置**（右上角）：为任一类实体增删自定义字段定义。

### 与全站打通的点

1. **CRM 事件 → 统一时间轴**：新建实体、阶段流转、每次跟踪、关键日期都会 `recordTimelineEvent`，
   于是出现在日历"当天记录"与原生「时间线」数据库里；
2. **CRM ↔ 首页记录**：跟踪记录可"同时生成一篇记录"（落在「笔记」容器下，标题=实体名·日期），
   事件里保留 `linkedViewId` 可一键跳回；也可手动"关联记录"把已有页面挂到实体上（`crm_links`）；
3. **旧数据迁移**：v1 的 `crm_customers` 首次进入 CRM 时自动迁入通用实体表（幂等）。

### 真机验证 / 待确认

| 项 | 状态 |
| --- | --- |
| CRM 六个分类标签（贴左 + 下划线跟随） | ✔ 真机确认 |
| 新建弹层（按类型裁剪字段 + 阶段 chips + 关键日期） | ✔ 真机确认渲染 |
| 记录/编辑/转项目/生成记录页 | ⏳ 需手填一次（adb 输入被输入法占用） |

## 2026-09-25 · 步骤㉛：CRM 关系模型按单人使用场景调整

**用户明确的关系语义**：

| 关系 | 基数 | 实现 |
| --- | --- | --- |
| 客户 ↔ 联系人 | **多对多**（一个联系人可服务多个单位；一个客户可有多个联系人） | 新表 `crm_relations`（双向写入，两边都能查到） |
| 客户 → 项目 | 一对多（同一客户多期多个项目） | `project.customer_id` + 客户详情里派生展示 |
| 客户 → 合同 | 一对多 | `contract.customer_id` + 客户详情里派生展示 |
| 项目 → 客户 | **一对一** | 项目详情「客户（一对一）」单选，选择器带清空 |
| 项目 → 合同 | **一对一** | 合同详情选项目时**查重**：该项目已有其它合同则拦截提示；项目详情也显示其合同 |
| 收款 → 合同 | 一对一 | 新增 `receivable.contract_id`（v3 迁移加列）；选合同后**自动带出**项目与客户 |
| 负责人 | —— | **默认隐藏**（数据库列保留，便于将来多人协作时再开启） |

### 本轮改动

1. `app_crm_biz` 迁移 **v3**：新增 `crm_relations`（含双向索引）与 `crm_entities.contract_id`；
2. 仓储新增：`relationsOf / linkEntities / unlinkEntities / listByColumn`；
3. 详情页新增「**关联**」区块：按类型给出该有的入口（客户：联系人 + 项目/合同/收款派生；
   联系人：所属客户；项目：客户 + 合同 + 联系人；合同：项目 + 客户；收款：合同；线索：客户 + 联系人）；
4. 关联项可直接点进对应实体详情（`openCrmEntity`）；
5. 新建弹层与详情页**移除负责人字段**（真机确认新建线索弹层已不再显示负责人）。

**待手动确认**：客户↔联系人多对多的添加/移除、项目选客户、合同选项目（含重复拦截）、收款选合同自动带出。

## 2026-09-25 · 步骤㉜：每类实体的**预置字段**（非必填、可自定义）

**用户要求**：线索里的"客户/联系人/电话"等不是必选项，但要有**预设字段**；
客户/联系人/项目/合同等也要有合理设计的常见字段。

### 预置字段清单（首次进入 CRM 自动种入，只种一次）

| 实体 | 预置字段 |
| --- | --- |
| **线索** | 来源(展会/转介绍/网络推广/电话咨询/老客户/其他)、意向单位、对接人、电话、微信、优先级(高/中/低)、预计金额(元)、预计成交日期、地区、关闭原因(价格/工期/资质/竞品/无预算/其他) |
| **客户** | 行业(市政/水务/化工/制药/食品/电力/其他)、客户等级(A 重点/B 一般/C 潜在)、地区、总机/电话、地址、纳税人识别号、开票信息、客户来源、付款习惯 |
| **联系人** | 职务、电话、手机、微信、邮箱、决策角色(决策人/使用人/影响者/采购/财务)、生日、爱好 |
| **项目** | 项目地点、规模/处理量、赢率(%)、投标日期、期望交付日期、竞品情况、跟进策略 |
| **合同** | 合同编号、生效日期、到期日期、付款方式(预付/进度款/到货款/验收款/质保金)、质保期、发票类型(专票13%/专票6%/普票)、我方签约主体 |
| **收款** | 已收金额(元)、收款方式(银行转账/承兑/现金/其他)、发票号、开票日期、开票金额(元) |

设计原则：**只放平时真会用的**；已经作为核心列存在的（标题/阶段/金额/关键日期/备注/客户与项目外键）
不重复建字段，避免同一信息两处维护；能枚举的一律做成 `select`（手机上点一下比打字快）。

### 实现

1. `app_crm_biz` 迁移 **v4**：新增 `crm_seed_state`（种入标记）；
2. `CrmEntityRepository.seedPresetFields()`：首次运行把预置字段写入 `crm_field_defs`，
   用标记表保证**只种一次**（用户改名/删除后不会被复活）；CRM 首页初始化时调用；
3. 详情页字段区**按类型渲染编辑器**：
   - `text/number` → 输入框（数字键盘）
   - `date` → 日期选择器
   - `select` → 选项 chips（含"清空"）
4. 新建弹层精简为「标题 / 摘要 / (金额) / 阶段 / 关键日期 / 备注」，
   **负责人与电话不再出现在新建流程**（电话已归入预置字段，非必填）；
5. 右上角「字段设置」仍可增删自己的字段（值存在实体 `extra`，不改表结构）。

真机确认：新建线索弹层已不再显示负责人/电话（只剩阶段、关键日期、备注、保存）。

## 2026-09-25 · 步骤㉝：对多关联改为「折叠 + ＋下拉选择」+ CRM 接入 AI 记忆

### ① 对多关联的展示方式（用户要求）

之前把关联项全部铺开，条目一多就占满屏幕。现在：

- **对多**（客户 ↔ 联系人；客户 → 项目/合同/收款）：
  一行显示「名称 + N 项 + 展开箭头 + **＋**」；
  - 点行 → 展开/收起明细（明细里可移除、可点进对应实体）；
  - 点 **＋** → 弹出**下拉选择列表**挑选实体添加（`_pickEntity`）；
- **对一**（项目 → 客户/合同；合同 → 项目/客户；收款 → 合同；线索 → 客户）：
  单值行「名称 + 当前值 + ＋选择 + ×清空」，不再列表化。

真机确认（用户已有数据「四川石化75立方每小时」线索详情）：
「联系人 1 项 ⌄ ＋」「客户 四川石化 ＋ ×」显示正常 ✔

### ② CRM 接入 AI 记忆（"全打通"的下一步）

- 详情页新增「**AI 记忆**」卡：一键生成/更新该实体的摘要与标签；
- 生成逻辑：把 实体核心字段 + 自定义字段 + 全部跟踪记录 拼成文本 → 本地抽取式摘要器
  （`TextDigester`，离线）→ 存入统一记忆库 `ai_digests`，**来源 id = `crm:<entityId>`**；
- 因为写的是同一个记忆库，**「AI 记忆」标签页里也能看到这条 CRM 记忆**，可与笔记/日记的记忆一起检索；
- 为支持指定来源，`app_ai_ext` 新增 `AiService.digestTextWithSource(sourceId, title, text)`。

真机确认：线索详情点「生成摘要」→ 卡片出现摘要与 `#转项目 #四川石化 #线索` 等标签 ✔

**已知可优化**：跟踪记录内容重复时，抽取式摘要会出现重复句子（对同一句话反复出现的内容，
后续可在摘要器里加"相似句去重"）。

## 2026-09-25 · 步骤㉞：CRM 带格式输入框（日期/时间分离、电话、邮箱、单位与字数常驻）

**用户要求**：时间、日期、电话、邮箱等有格式的字段要用**带格式化的输入框**；
右侧信息**常驻**；日期与时间**自动分隔**；显示**单位与字数**；整体**浅色**样式。

### 新增 `lib/extensions/local_home/crm_field_inputs.dart`

| 控件 | 行为 |
| --- | --- |
| `CrmTextField` | 通用文本/数字输入：**右侧常驻**「单位 + 已输入/上限」（浅色，随输入实时更新）；字数上限用 `LengthLimitingTextInputFormatter` 真正限制 |
| `CrmPhoneField` | 电话：`PhoneInputFormatter` 自动按 **3-4-4 分组**、只留数字、最多 11 位；提示 `138 0000 0000` |
| `CrmEmailField` | 邮箱：禁空白字符、邮箱键盘，非空时做格式校验，**浅色**错误提示 |
| `CrmDateField` | 日期 / 日期时间：点击选择；**日期与时间分段显示**（`2026-09-25 │ 14:30`，两段分别选中，不会糊成一串数字） |
| `formatCrmDate / parseCrmDate` | 统一 `yyyy-MM-dd` 与 `yyyy-MM-dd HH:mm` 的格式化/解析 |

### 字段定义扩展

- `CrmFieldDef` 新增 `unit`（右侧单位，如 元 / % / 月）与 `maxLength`（字数上限）；
- `type` 扩展出 `phone` / `email` / `datetime`；
- **预置字段**相应升级：电话/手机 → `phone`、邮箱 → `email`、
  预计金额/已收金额/开票金额 → 单位「元」、赢率 → 单位「%」、质保期 → 单位「月」、
  纳税人识别号（20 字）/ 地址（80 字）等带字数上限。

### 接入位置

1. **实体详情**字段编辑：按类型打开对应的格式化输入面板（文本/数字/电话/邮箱/日期/日期时间/单选）；
2. **新建实体弹层**：底部新增「更多字段（可选填）」，直接按预置字段渲染对应输入控件，
   新建时即可用格式化输入框填写（值写入实体 `extra`）。

**说明**：本轮的视觉细节（单位/字数是否如预期常驻、浅色）在真机上我这边 adb 点击坐标不稳、
没截到那一帧，需要你手动点一下「预计金额（元）」与「纳税人识别号」确认；
如果字号/颜色想再淡一点或换成"右下角小字"，告诉我具体样式我再调。

## 2026-09-25 · 步骤㉟：CRM 内部 UI —— 用 design-interface 技能做方案探索（未动代码）

**过程**：按 `design-interface` 技能流程执行：简报 → 定维度 → 并行探索 → 对比 → 待决策。

**降级说明（重要）**：技能要求用并行子代理跑"互不见面"的设计角落，但本环境的**子代理任务投递失败**
（连试 3 个代理 + 重发 brief 均收不到正文，子代理回复"没有任务内容"），因此按技能里写明的降级方案执行：
**由同一执行者以四位互不见面的设计师的方式，把四个角落各自写死一遍**。

**产出**：`doc/CRM手机端UI设计方案对比.md`，四个差异化的方案 + 对比矩阵 + 可混搭组合：

| 方案 | 角落（导航/密度/节奏） | 一句话 |
| --- | --- | --- |
| A 客户全貌 | 客户 Hub／宽松聚焦／确认式 | CRM 只有一层结构＝客户，点进去看"这个人的全部" |
| B 漏斗流 | 流程管道／高密度仪表盘／拖拽 | 首页就是阶段看板，拖一下＝推进阶段 |
| C 跟进流 | 活动时间优先／中等密度／手势环境式 | 首页是"今天该跟进谁＋最近发生了什么"，与日历同源 |
| D 输入即达 | 搜索命令／低密度大目标／键盘+确认（无障碍优先） | 一个输入框，输入即达；大字号高对比，单卡聚焦 |

**下一步**：等用户选定方向（或混搭），再按 `grill-me` 把边界问清，然后写实现方案并落地。
