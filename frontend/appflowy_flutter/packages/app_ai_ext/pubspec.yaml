name: app_ai_ext
description: AppFlowy 二次开发扩展包 —— AI 扩展（自有模型接入 + 记忆体系编排）
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.4"

dependencies:
  flutter:
    sdk: flutter
  # 独立业务库（AI 记忆库表 ai_digests，与内核 Core 库分离）
  app_biz_store:
    path: ../app_biz_store
  appflowy_backend:
    path: ../appflowy_backend
  appflowy_result:
    path: ../appflowy_result
